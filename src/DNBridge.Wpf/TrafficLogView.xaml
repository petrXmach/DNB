<UserControl x:Class="DNBridge.Wpf.TrafficLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DNBridge.Wpf">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- 0: legend -->
            <RowDefinition Height="*"/>      <!-- 1: master list -->
            <RowDefinition Height="Auto"/>   <!-- 2: splitter -->
            <RowDefinition Height="140"/>    <!-- 3: detail pane -->
        </Grid.RowDefinitions>

        <!-- Legend -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="2,2,2,4">
            <TextBlock Text="◀  received" Foreground="#1565C0" FontFamily="Consolas" FontSize="11" FontWeight="Bold" Margin="0,0,16,0"/>
            <TextBlock Text="▶  sent" Foreground="#B71C1C" FontFamily="Consolas" FontSize="11" FontWeight="Bold"/>
        </StackPanel>

        <!-- Master: one summary line per frame -->
        <ListBox Grid.Row="1" x:Name="MasterList"
                 SelectionChanged="MasterList_SelectionChanged"
                 Background="#F5F5F5"
                 FontFamily="Consolas"
                 FontSize="11"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type local:TrafficItem}">
                    <TextBlock Text="{Binding Summary}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#1565C0"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Outbound}" Value="True">
                                        <Setter Property="Foreground" Value="#B71C1C"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <GridSplitter Grid.Row="2" Height="4" HorizontalAlignment="Stretch" Background="#DDDDDD"/>

        <!-- Detail pane: full text of the selected frame -->
        <TextBox Grid.Row="3" x:Name="DetailBox"
                 IsReadOnly="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 Background="#FAFAFA"
                 FontFamily="Consolas"
                 FontSize="11"/>
    </Grid>
</UserControl>
